This tool currently lets users configure slick-greeter. Its scope can
(should/will?) be extended to configure the LightDM display manager
itself and to detect and configure other greeters such as
lightdm-gtk-greeter.
